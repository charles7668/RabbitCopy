<Window x:Class="RabbitCopy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:viewModels="clr-namespace:RabbitCopy.ViewModels"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel, IsDesignTimeCreatable=True}"
        Icon="{Binding WindowIcon}"
        Title="RabbitCopy" Height="500" Width="505">
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Loaded">
            <b:InvokeCommandAction Command="{Binding WindowLoadedCommand}" />
        </b:EventTrigger>
    </b:Interaction.Triggers>

    <Grid>
        <ScrollViewer>
            <StackPanel HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Button Grid.Row="0" Grid.Column="0" Content="Src" Command="{Binding SelectSourcesCommand}" />
                    <TextBox Grid.Row="0" Grid.Column="1"
                             Style="{StaticResource TextBoxExtend}"
                             Text="{Binding SrcText}"
                             hc:InfoElement.Title="Source"
                             VerticalAlignment="Center"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             MaxHeight="200" />

                    <Button Grid.Row="1" Grid.Column="0" Content="Dest"
                            Command="{Binding SelectDestDirCommand}" />
                    <TextBox Grid.Row="1" Grid.Column="1"
                             Style="{StaticResource TextBoxExtend}"
                             hc:InfoElement.Title="Destination"
                             Text="{Binding DestText}"
                             VerticalAlignment="Center" />
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <hc:TextBox Height="200" IsReadOnly="True"
                                Text="{Binding CopyLog}"
                                TextWrapping="Wrap" />
                    <Grid Grid.Column="1">
                        <Border Padding="10">
                            <hc:SimpleStackPanel HorizontalAlignment="Stretch">
                                <CheckBox HorizontalAlignment="Left" IsChecked="{Binding ExcludeEmptyDirsOption}">
                                    Exclude Empty Directories
                                </CheckBox>
                                <hc:SimpleStackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Command="{Binding ExecuteCopyCommand}">Execute</Button>
                                    <Button Command="{Binding DryRunCopyCommand}">Dry Run</Button>
                                </hc:SimpleStackPanel>
                            </hc:SimpleStackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>